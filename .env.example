# ============================================
# BACKEND TUTORIA - VARIABLES DE ENTORNO
# ============================================
# Copia este archivo como ".env" y configura
# los valores apropiados para tu entorno.
# ============================================

# ============================================
# BASE DE DATOS
# ============================================
# URL de conexión a PostgreSQL
# Formato: postgresql://usuario:password@host:puerto/nombre_bd
DATABASE_URL=postgresql://postgres:password@localhost:5432/tutoria

# ============================================
# SEGURIDAD - JWT
# ============================================
# ⚠️ IMPORTANTE: Genera una clave secreta aleatoria y segura
# Puedes generarla con: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=tu-clave-secreta-muy-larga-y-aleatoria-cambiar-esto

# Algoritmo de encriptación para JWT (no cambiar a menos que sepas lo que haces)
ALGORITHM=HS256

# Tiempo de expiración del token en minutos
ACCESS_TOKEN_EXPIRE_MINUTES=60

# ============================================
# SERVIDOR
# ============================================
# Host y puerto del servidor (opcional, por defecto 0.0.0.0:8000)
# HOST=0.0.0.0
# PORT=8000

# Modo de desarrollo o producción
# ENVIRONMENT=development

# Modo debug (True en desarrollo, False en producción)
# ⚠️ En modo DEBUG, el token de reset se incluye en la respuesta de la API
# ⚠️ En producción, SIEMPRE debe ser False
DEBUG=True

# ============================================
# CORS - Origenes Permitidos
# ============================================
# Lista de origenes permitidos separados por coma
# CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# ============================================
# EMAIL (para reset password, notificaciones)
# ============================================
# Proveedor de email: smtp, sendgrid, dev (development)
# - smtp: Usar servidor SMTP (Gmail, Outlook, etc.)
# - sendgrid: Usar API de SendGrid
# - dev: Modo desarrollo (solo imprime en consola, no envía emails)
EMAIL_PROVIDER=dev

# Email del remitente (aparecerá como "De:")
EMAIL_FROM=noreply@tutoria.com

# URL del frontend (para enlaces en emails)
FRONTEND_URL=http://localhost:3000

# ============================================
# CONFIGURACIÓN SMTP (si EMAIL_PROVIDER=smtp)
# ============================================
# Gmail:
#   SMTP_HOST=smtp.gmail.com
#   SMTP_PORT=587
#   SMTP_USER=tu-email@gmail.com
#   SMTP_PASSWORD=tu-app-password  (NO tu contraseña normal, usa App Password)
#   SMTP_USE_TLS=True
#
# Outlook/Hotmail:
#   SMTP_HOST=smtp.office365.com
#   SMTP_PORT=587
#   SMTP_USER=tu-email@outlook.com
#   SMTP_PASSWORD=tu-password
#   SMTP_USE_TLS=True
#
# Configuración personalizada:
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=tu-email@gmail.com
# SMTP_PASSWORD=tu-password-de-aplicacion
# SMTP_USE_TLS=True

# ============================================
# CONFIGURACIÓN SENDGRID (si EMAIL_PROVIDER=sendgrid)
# ============================================
# API Key de SendGrid (obtén una en: https://sendgrid.com)
# SENDGRID_API_KEY=SG.xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ============================================
# ARCHIVOS Y ALMACENAMIENTO
# ============================================
# Directorio base para uploads
# UPLOAD_DIR=./uploads

# Tamaño máximo de archivo en MB
# MAX_FILE_SIZE_MB=10

# ============================================
# IA Y MODELOS
# ============================================
# Modelo de Whisper a usar (tiny, base, small, medium, large)
WHISPER_MODEL=small

# Dispositivo para IA (cpu, cuda)
# AI_DEVICE=cpu

# ============================================
# LOGGING
# ============================================
# Nivel de logging (DEBUG, INFO, WARNING, ERROR, CRITICAL)
# LOG_LEVEL=INFO

# Directorio de logs
# LOG_DIR=./logs

# ============================================
# CACHE Y RENDIMIENTO
# ============================================
# URL de Redis (si se usa para cache)
# REDIS_URL=redis://localhost:6379/0

# ============================================
# NOTAS IMPORTANTES
# ============================================
# 1. NO subas el archivo .env al repositorio
# 2. .env debe estar en .gitignore
# 3. Genera una SECRET_KEY única y segura
# 4. Cambia todos los valores por defecto en producción
# 5. Usa variables de entorno del sistema en producción (no archivo .env)
